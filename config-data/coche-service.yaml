server:
  port: 8080
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
      service-uri:
        default-zone: hhtp://localhost:8761/eureka
      instance:
        hostname: localhost
      