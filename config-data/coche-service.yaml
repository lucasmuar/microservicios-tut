server:
  port: 8080
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
      service-url:
        default-zone: hhtp://localhost:8765/eureka
      instance:
        hostname: localhost
      